---
phase: 04-testing-verification
plan: 03
type: tdd
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - tests/test_providers_smoke.py
autonomous: true
requirements:
  - REQ-031
must_haves:
  truths:
    - "Each of the 7 providers has a smoke test for list_directory that returns a list of RemoteItem"
    - "Each provider that implements upload_file has a smoke test verifying it calls the underlying SDK upload method"
    - "No real network calls are made — all SDK clients are mocked"
    - "All provider smoke tests pass with pytest"
  artifacts:
    - path: "tests/test_providers_smoke.py"
      provides: "smoke tests for all 7 providers (dropbox, google_drive, onedrive, webdav, ftp, s3, sftp placeholder)"
      min_lines: 200
  key_links:
    - from: "tests/test_providers_smoke.py"
      to: "providers/dropbox.py"
      via: "patch('dropbox.Dropbox')"
      pattern: "DropboxProvider"
    - from: "tests/test_providers_smoke.py"
      to: "providers/google_drive.py"
      via: "patch('googleapiclient.discovery.build')"
      pattern: "GoogleDriveProvider"
---

<objective>
Integration smoke tests for all provider implementations — list_directory and upload_file — with all external SDK calls mocked.

Purpose: Catch regressions in the provider→SDK wiring introduced in Phase 1 without requiring real cloud credentials.
Output: tests/test_providers_smoke.py
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@providers/base.py
@providers/dropbox.py
@providers/google_drive.py
@providers/onedrive.py
@providers/webdav.py
@providers/ftp.py
@providers/s3.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: read provider internals before writing mocks</name>
  <files>tests/test_providers_smoke.py</files>
  <read_first>
    - providers/dropbox.py (full file — understand list_directory and _chunked_upload / upload_file SDK calls)
    - providers/google_drive.py (full file — understand list_directory: service.files().list() and upload_file)
    - providers/onedrive.py (full file — understand list_directory: msal + requests and upload_file)
    - providers/webdav.py (full file — understand list_directory: webdavclient3 and upload_file)
    - providers/ftp.py (full file — understand list_directory: ftplib and upload_file)
    - providers/s3.py (full file — understand list_directory: boto3.client and upload_file)
    - providers/base.py (RemoteItem dataclass fields: name, path, is_dir, size, modified, mime_type, icon)
  </read_first>
  <behavior>
    READ ALL PROVIDER FILES FIRST. Then implement the following smoke tests:

    ### Dropbox
    - test_dropbox_list_directory: patch `dropbox.Dropbox`; configure mock to return a `files_list_folder` result with one FileMetadata entry (name="test.fcstd", path_display="/test.fcstd", size=100, client_modified=datetime(2024,1,1)); call provider.list_directory("/"); assert result is a list of RemoteItem with name="test.fcstd"
    - test_dropbox_upload_file: patch `dropbox.Dropbox` and open; configure mock upload_session_start/append_v2/finish; call provider.upload_file(local_path, "/"); assert SDK finish method was called once

    ### Google Drive
    - test_gdrive_list_directory: patch `googleapiclient.discovery.build`; configure service.files().list().execute() to return {"files": [{"id":"abc","name":"part.fcstd","mimeType":"application/octet-stream","size":"512"}]}; assert result list has one RemoteItem
    - test_gdrive_upload_file: patch `googleapiclient.discovery.build` and `google.oauth2.credentials.Credentials`; configure mock MediaFileUpload and files().create().execute(); assert files().create() was called

    ### OneDrive
    - test_onedrive_list_directory: patch `msal.PublicClientApplication` and `requests.get`; configure requests.get to return JSON {"value": [{"name":"doc.fcstd","file":{},"size":200,"lastModifiedDateTime":"2024-01-01T00:00:00Z","id":"1"}]}; assert result has one RemoteItem
    - test_onedrive_upload_file: patch `requests.put` to return status_code=200; assert requests.put was called with a URL containing the filename

    ### WebDAV
    - test_webdav_list_directory: patch `webdav3.client.Client`; configure mock list() to return [".", "file.fcstd"]; configure mock info("file.fcstd") to return {"size":"100","modified":"Mon, 01 Jan 2024 00:00:00 GMT","isdir":"0"}; assert result has one RemoteItem
    - test_webdav_upload_file: patch `webdav3.client.Client`; assert mock upload_sync() was called

    ### FTP
    - test_ftp_list_directory: patch `ftplib.FTP`; configure MLSD to yield [("file.fcstd", {"type":"file","size":"50","modify":"20240101000000"})] and one dir entry; assert result has two RemoteItem
    - test_ftp_upload_file: patch `ftplib.FTP`; assert storbinary was called

    ### S3
    - test_s3_list_directory: patch `boto3.client`; configure list_objects_v2 to return {"Contents":[{"Key":"folder/file.fcstd","Size":300,"LastModified":datetime(2024,1,1,tzinfo=timezone.utc)}],"CommonPrefixes":[{"Prefix":"folder/sub/"}]}; assert result includes both file and folder RemoteItem
    - test_s3_upload_file: patch `boto3.client`; assert upload_file (or put_object) was called on the mock S3 client
  </behavior>
  <action>
READ ALL PROVIDER SOURCE FILES LISTED IN read_first BEFORE WRITING ANY TEST CODE.

After reading, create `tests/test_providers_smoke.py`.

Structure: one test class per provider (class TestDropboxSmoke, class TestGoogleDriveSmoke, etc.) or flat functions — your choice. Use `unittest.mock.patch` as decorators or context managers.

General mock setup pattern for each provider:
1. Instantiate the provider with a minimal config dict (no real credentials needed): `provider = XxxProvider({"name": "test", ...required_keys_with_fake_values...})`
2. Set `provider._authenticated = True` and assign the mock SDK client to the provider's internal client attribute (e.g., `provider._dbx = mock_dbx`) to bypass the authenticate() call entirely.
3. Call the method under test.
4. Assert on the return value AND that the expected SDK method was called.

For providers where the SDK client is constructed lazily in list_directory (not stored as self._xxx), patch the SDK import at the module level (e.g., `patch("providers.onedrive.requests.get", ...)`).

Import required datetime utilities:
```python
from datetime import datetime, timezone
from unittest.mock import MagicMock, patch, mock_open
import pytest
from providers.base import RemoteItem
```

Do NOT import FreeCAD — providers fall back gracefully when it is absent.
  </action>
  <verify>
    <automated>python -m pytest tests/test_providers_smoke.py -v 2>&1</automated>
  </verify>
  <done>All 12 provider smoke tests pass; 0 failures, 0 errors; no real network calls made</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → mock SDK clients | No real API credentials or network calls; all SDK objects are MagicMock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Info Disclosure | fake credentials in test | accept | strings like "fake_token" carry no real secrets |
| T-04-06 | Spoofing | mock SDK returning controlled data | accept | this is the intent of smoke tests; real integration requires live credentials |
</threat_model>

<verification>
```
python -m pytest tests/test_providers_smoke.py -v
```
All 12 tests collected and pass. No imports of FreeCAD required.
</verification>

<success_criteria>
- tests/test_providers_smoke.py: 12 smoke tests (2 per provider × 6 providers), all pass
- No real credentials, network calls, or FreeCAD import required
- All 7 providers covered (dropbox, google_drive, onedrive, webdav, ftp, s3; sftp/paramiko if upload_file is implemented)
- `python -m pytest tests/test_providers_smoke.py` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-testing-verification/04-03-SUMMARY.md`

Then run the full suite to confirm nothing regressed:
```
python -m pytest tests/ -v
```
</output>
