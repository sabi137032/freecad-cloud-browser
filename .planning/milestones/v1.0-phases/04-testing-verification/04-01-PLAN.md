---
phase: 04-testing-verification
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config_store.py
  - tests/test_file_cache.py
autonomous: true
requirements:
  - REQ-030
must_haves:
  truths:
    - "pytest discovers and runs all tests in tests/ with zero failures"
    - "config_store tests cover: deepcopy isolation, config_dir property, atomic save, JSON corruption recovery, account CRUD"
    - "file_cache tests cover: cache key determinism, is_cached staleness logic (ISO-8601 and Unix ts), invalidate, clear_provider, cache_size_bytes"
  artifacts:
    - path: "tests/__init__.py"
      provides: "package marker"
    - path: "tests/conftest.py"
      provides: "shared pytest fixtures (tmp_path-based ConfigStore and FileCache)"
    - path: "tests/test_config_store.py"
      provides: "unit tests for ConfigStore"
      min_lines: 80
    - path: "tests/test_file_cache.py"
      provides: "unit tests for FileCache"
      min_lines: 60
  key_links:
    - from: "tests/conftest.py"
      to: "core/config_store.py"
      via: "ConfigStore(tmp_path / 'config.json')"
      pattern: "ConfigStore"
    - from: "tests/conftest.py"
      to: "core/file_cache.py"
      via: "FileCache(str(tmp_path / 'cache'))"
      pattern: "FileCache"
---

<objective>
Unit tests for ConfigStore and FileCache — both pure-Python modules with no external SDK deps.

Purpose: Verify the data-layer invariants fixed in Phase 1 (deepcopy isolation, atomic save, staleness checks) remain correct and catch regressions.
Output: tests/conftest.py (shared fixtures), tests/test_config_store.py, tests/test_file_cache.py
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@core/config_store.py
@core/file_cache.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: scaffold tests package and shared fixtures</name>
  <files>tests/__init__.py, tests/conftest.py</files>
  <read_first>
    - core/config_store.py (understand ConfigStore constructor — accepts optional config_path)
    - core/file_cache.py (understand FileCache constructor — accepts optional cache_dir)
  </read_first>
  <behavior>
    - fixture `config_store` returns a ConfigStore(tmp_path / "cfg" / "config.json") with the parent dir pre-created
    - fixture `cache` returns a FileCache(str(tmp_path / "cache"))
    - both fixtures are function-scoped (fresh per test)
    - no FreeCAD import required — both modules fall back to os.path.expanduser / tempfile when FreeCAD is absent
  </behavior>
  <action>
Create `tests/__init__.py` as an empty file.

Create `tests/conftest.py`:

```python
import os
import pytest
from core.config_store import ConfigStore
from core.file_cache import FileCache


@pytest.fixture()
def config_store(tmp_path):
    cfg_dir = tmp_path / "cfg"
    cfg_dir.mkdir()
    return ConfigStore(config_path=str(cfg_dir / "config.json"))


@pytest.fixture()
def cache(tmp_path):
    return FileCache(cache_dir=str(tmp_path / "cache"))
```
  </action>
  <verify>
    <automated>python -m pytest tests/conftest.py --collect-only -q 2>&1 | findstr /V "no tests"</automated>
  </verify>
  <done>conftest.py importable with no errors; both fixtures resolvable</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: unit tests for ConfigStore (REQ-001)</name>
  <files>tests/test_config_store.py</files>
  <read_first>
    - core/config_store.py (all public methods: config_dir, list_accounts, get_account, save_account, add_account, delete_account, get_setting, set_setting, _save)
    - tests/conftest.py (config_store fixture)
  </read_first>
  <behavior>
    - test_config_dir_is_directory: config_store.config_dir is a valid directory path (os.path.isdir returns True)
    - test_list_accounts_empty: fresh store returns []
    - test_add_and_list: add_account returns a UUID string; list_accounts returns list of length 1 with 'id' key present
    - test_get_account_deepcopy: mutating the returned dict does NOT affect subsequent get_account calls (deepcopy isolation — REQ-001)
    - test_list_accounts_deepcopy: mutating an item from list_accounts does NOT affect subsequent list_accounts calls
    - test_delete_account: after delete_account, get_account returns None
    - test_save_persists: after save_account, a NEW ConfigStore pointed at the same path returns the same data
    - test_atomic_save_on_corrupted_json: write garbage to config.json before constructing ConfigStore; expect empty accounts dict (not an exception)
    - test_get_set_setting: set_setting then get_setting round-trips the value; persists across reload
    - test_save_raises_on_permission_error: patch os.replace to raise PermissionError; calling set_setting raises PermissionError (bare re-raise — not wrapped)
  </behavior>
  <action>
Create `tests/test_config_store.py` implementing all ten tests above using pytest and unittest.mock.patch.

Key patterns:
- Use `config_store` fixture (from conftest.py) for all tests.
- For test_save_persists and test_atomic_save_on_corrupted_json, construct a second `ConfigStore(config_path=config_store._path)`.
- For test_get_account_deepcopy: call `add_account`, get the returned id, call `get_account(id)`, mutate the dict (e.g., `result["name"] = "MUTATED"`), then assert `config_store.get_account(id)["name"] != "MUTATED"`.
- For test_save_raises_on_permission_error: use `unittest.mock.patch("os.replace", side_effect=PermissionError("disk full"))` as a context manager; call `config_store.set_setting("x", 1)` inside and assert `pytest.raises(PermissionError)`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_config_store.py -v 2>&1</automated>
  </verify>
  <done>All 10 tests pass; 0 failures, 0 errors</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: unit tests for FileCache (REQ-001 adjacent)</name>
  <files>tests/test_file_cache.py</files>
  <read_first>
    - core/file_cache.py (all public methods: get_local_path, is_cached, invalidate, clear_provider, clear_all, cache_size_bytes, _cache_key)
    - tests/conftest.py (cache fixture)
  </read_first>
  <behavior>
    - test_cache_key_deterministic: _cache_key("dropbox", "/a/b.fcstd") called twice returns same 20-char hex string
    - test_cache_key_differs_by_provider: _cache_key("dropbox", "/x") != _cache_key("s3", "/x")
    - test_get_local_path_creates_dir: get_local_path("dropbox", "/foo", "bar.fcstd") returns a string whose parent directory exists on disk
    - test_is_cached_false_when_missing: is_cached returns False for a path that was never written
    - test_is_cached_true_when_file_exists: write a file to get_local_path(); is_cached returns True with remote_modified=None
    - test_is_cached_stale_when_remote_newer: write a file, then call is_cached with remote_modified set to a future ISO-8601 timestamp (e.g., "2099-01-01T00:00:00Z"); expect False
    - test_is_cached_fresh_when_remote_older: call is_cached with remote_modified set to an old timestamp (e.g., "2000-01-01T00:00:00Z"); expect True
    - test_is_cached_unix_timestamp: call is_cached with remote_modified="0" (Unix epoch); a file written now is newer → True
    - test_invalidate_removes_file: write a file then invalidate; is_cached returns False
    - test_clear_provider_removes_dir: write a file under "dropbox", call clear_provider("dropbox"); directory no longer exists
    - test_cache_size_bytes: write two files of known size; cache_size_bytes() returns sum ≥ their combined size
  </behavior>
  <action>
Create `tests/test_file_cache.py` implementing all 11 tests using pytest. Use the `cache` fixture from conftest.py.

Key patterns:
- Write actual files using `Path(cache.get_local_path(...)).write_bytes(b"x" * N)` to create cache entries.
- For timestamp tests, use `datetime.now(timezone.utc).isoformat()` for "now" and string literals for past/future.
- For test_cache_size_bytes: write two files with `b"A" * 100` and `b"B" * 200`; assert `cache.cache_size_bytes() >= 300`.
- Import `from core.file_cache import FileCache` directly (not via fixture) for static method tests (_cache_key).
  </action>
  <verify>
    <automated>python -m pytest tests/test_file_cache.py -v 2>&1</automated>
  </verify>
  <done>All 11 tests pass; 0 failures, 0 errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → filesystem | Tests write/read temp files; tmp_path fixture scopes isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | test tmp files | accept | pytest tmp_path is per-test; no cross-test pollution |
| T-04-02 | Info Disclosure | config.json in tmp | accept | tmp_path is under OS temp dir, auto-cleaned by pytest |
</threat_model>

<verification>
```
python -m pytest tests/test_config_store.py tests/test_file_cache.py -v
```
All tests collected and pass. No import errors.
</verification>

<success_criteria>
- tests/__init__.py exists
- tests/conftest.py contains config_store and cache fixtures
- tests/test_config_store.py: 10 tests, all pass
- tests/test_file_cache.py: 11 tests, all pass
- `python -m pytest tests/test_config_store.py tests/test_file_cache.py` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-testing-verification/04-01-SUMMARY.md`
</output>
